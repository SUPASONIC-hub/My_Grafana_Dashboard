services:
  - type: web
    runtime: static
    name: yozm-wedding-dashboard-portfolio
    buildCommand: echo "Static portfolio site ready"
    staticPublishPath: .
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
