services:
  - type: web
    runtime: static
    name: my-grafana-dashboard
    buildCommand: echo "Static portfolio site ready"
    staticPublishPath: .
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
